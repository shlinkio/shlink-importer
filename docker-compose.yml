
version: '3'

services:
  shlink_importer_php:
    container_name: shlink_importer_php
    build:
      context: .
    volumes:
      - ./:/app
    extra_hosts:
      - 'host.docker.internal:host-gateway'
